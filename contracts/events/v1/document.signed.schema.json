{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "document.signed.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["event_id", "event_type", "routing_key", "version", "occurred_at", "aggregate_type", "aggregate_id", "actor_id", "data"],
  "properties": {
    "event_id": { "type": "string", "minLength": 1 },
    "event_type": { "const": "document.signed" },
    "routing_key": { "const": "document.signed.v1" },
    "version": { "const": 1 },
    "occurred_at": { "type": "string", "minLength": 1 },
    "aggregate_type": { "const": "document" },
    "aggregate_id": { "type": "string", "minLength": 1 },
    "actor_id": { "type": ["integer", "null"] },
    "data": {
      "type": "object",
      "additionalProperties": false,
      "required": ["party_id", "signing_order", "signature_id"],
      "properties": {
        "party_id": { "type": "integer" },
        "signing_order": { "type": ["integer", "null"] },
        "signature_id": { "type": "integer" }
      }
    }
  }
}
